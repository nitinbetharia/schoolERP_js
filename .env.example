# Secrets only. Do not commit real values.
NODE_ENV=development
PORT=3000

# Database secrets
DB_USER=changeme
DB_PASSWORD=changeme
# Optional DSNs; override config URLs if set
DB_URL_MASTER=
DB_URL_TRUST=

# Crypto
SESSION_SECRET=changeme-session-secret
JWT_SECRET=changeme-jwt-secret
ENCRYPTION_KEY=changeme-32bytes-key

# Email (secrets only)
SMTP_USER=changeme
SMTP_PASSWORD=changeme
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
