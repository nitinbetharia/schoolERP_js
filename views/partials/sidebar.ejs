<aside class="sidebar bg-surface border-r border-border">
    <div class="p-4">
        <!-- Dashboard -->
        <div class="mb-6">
            <a href="/dashboard" class="flex items-center gap-3 p-3 rounded-lg hover:bg-surface-alt transition-colors <%= currentPath === '/dashboard' ? 'bg-primary-50 text-primary-600 border-primary-200' : '' %>">
                <i class="fas fa-tachometer-alt w-5"></i>
                <span class="font-medium">Dashboard</span>
            </a>
        </div>
        
        <!-- Navigation sections -->
        <nav class="space-y-6">
            <!-- Student Management -->
            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN', 'TEACHER'].includes(userRole)) { %>
                <div>
                    <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                        Student Management
                    </div>
                    <div class="space-y-1">
                        <a href="/students" class="nav-link <%= currentPath.startsWith('/students') ? 'active' : '' %>">
                            <i class="fas fa-user-graduate"></i>
                            <span>Students</span>
                        </a>
                        <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN'].includes(userRole)) { %>
                            <a href="/students/admissions" class="nav-link <%= currentPath === '/students/admissions' ? 'active' : '' %>">
                                <i class="fas fa-user-plus"></i>
                                <span>Admissions</span>
                            </a>
                        <% } %>
                    </div>
                </div>
            <% } %>
            
            <!-- Academic -->
            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN', 'TEACHER'].includes(userRole)) { %>
                <div>
                    <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                        Academic
                    </div>
                    <div class="space-y-1">
                        <a href="/attendance" class="nav-link <%= currentPath.startsWith('/attendance') ? 'active' : '' %>">
                            <i class="fas fa-calendar-check"></i>
                            <span>Attendance</span>
                        </a>
                        <% if (['TEACHER'].includes(userRole)) { %>
                            <a href="/attendance/mark" class="nav-link <%= currentPath === '/attendance/mark' ? 'active' : '' %>">
                                <i class="fas fa-check-circle"></i>
                                <span>Mark Attendance</span>
                            </a>
                        <% } %>
                        <a href="/attendance/leave" class="nav-link <%= currentPath === '/attendance/leave' ? 'active' : '' %>">
                            <i class="fas fa-calendar-times"></i>
                            <span>Leave Management</span>
                        </a>
                    </div>
                </div>
            <% } %>
            
            <!-- Financial -->
            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN', 'ACCOUNTANT', 'PARENT'].includes(userRole)) { %>
                <div>
                    <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                        Financial
                    </div>
                    <div class="space-y-1">
                        <% if (userRole === 'PARENT') { %>
                            <a href="/parent/fees" class="nav-link <%= currentPath === '/parent/fees' ? 'active' : '' %>">
                                <i class="fas fa-rupee-sign"></i>
                                <span>Fee Status</span>
                            </a>
                        <% } else { %>
                            <a href="/fees" class="nav-link <%= currentPath.startsWith('/fees') ? 'active' : '' %>">
                                <i class="fas fa-rupee-sign"></i>
                                <span>Fee Management</span>
                            </a>
                            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN'].includes(userRole)) { %>
                                <a href="/fees/structures" class="nav-link <%= currentPath === '/fees/structures' ? 'active' : '' %>">
                                    <i class="fas fa-list-alt"></i>
                                    <span>Fee Structures</span>
                                </a>
                            <% } %>
                            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN', 'ACCOUNTANT'].includes(userRole)) { %>
                                <a href="/fees/collection" class="nav-link <%= currentPath === '/fees/collection' ? 'active' : '' %>">
                                    <i class="fas fa-cash-register"></i>
                                    <span>Fee Collection</span>
                                </a>
                            <% } %>
                        <% } %>
                    </div>
                </div>
            <% } %>
            
            <!-- Administration -->
            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN'].includes(userRole)) { %>
                <div>
                    <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                        Administration
                    </div>
                    <div class="space-y-1">
                        <a href="/users" class="nav-link <%= currentPath.startsWith('/users') ? 'active' : '' %>">
                            <i class="fas fa-users"></i>
                            <span>User Management</span>
                        </a>
                        <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN'].includes(userRole)) { %>
                            <a href="/admin/schools" class="nav-link <%= currentPath === '/admin/schools' ? 'active' : '' %>">
                                <i class="fas fa-school"></i>
                                <span>Schools</span>
                            </a>
                        <% } %>
                        <% if (['SYSTEM_ADMIN'].includes(userRole)) { %>
                            <a href="/admin/setup" class="nav-link <%= currentPath === '/admin/setup' ? 'active' : '' %>">
                                <i class="fas fa-cogs"></i>
                                <span>System Setup</span>
                            </a>
                        <% } %>
                    </div>
                </div>
            <% } %>
            
            <!-- Enhanced Reports -->
            <div>
                <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                    Reports & Analytics
                </div>
                <div class="space-y-1">
                    <a href="/reports" class="nav-link <%= currentPath === '/reports' ? 'active' : '' %>">
                        <i class="fas fa-chart-bar"></i>
                        <span>Dashboard</span>
                    </a>
                    <a href="/reports/templates" class="nav-link <%= currentPath === '/reports/templates' ? 'active' : '' %>">
                        <i class="fas fa-file-alt"></i>
                        <span>Templates</span>
                    </a>
                    <a href="/reports/my-reports" class="nav-link <%= currentPath === '/reports/my-reports' ? 'active' : '' %>">
                        <i class="fas fa-user-chart"></i>
                        <span>My Reports</span>
                    </a>
                    <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN'].includes(userRole)) { %>
                        <a href="/reports/builder" class="nav-link <%= currentPath === '/reports/builder' ? 'active' : '' %>">
                            <i class="fas fa-tools"></i>
                            <span>Report Builder</span>
                        </a>
                        <a href="/reports/schedules" class="nav-link <%= currentPath === '/reports/schedules' ? 'active' : '' %>">
                            <i class="fas fa-clock"></i>
                            <span>Schedules</span>
                        </a>
                        <a href="/reports/analytics" class="nav-link <%= currentPath === '/reports/analytics' ? 'active' : '' %>">
                            <i class="fas fa-analytics"></i>
                            <span>Analytics</span>
                        </a>
                        <a href="/reports/dashboard-builder" class="nav-link <%= currentPath === '/reports/dashboard-builder' ? 'active' : '' %>">
                            <i class="fas fa-th-large"></i>
                            <span>Dashboard Builder</span>
                        </a>
                    <% } %>
                </div>
            </div>
            
            <!-- Communication -->
            <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN', 'STAFF'].includes(userRole)) { %>
                <div>
                    <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                        Communication
                    </div>
                    <div class="space-y-1">
                        <a href="/communication/bulk-send" class="nav-link <%= currentPath === '/communication/bulk-send' ? 'active' : '' %>">
                            <i class="fas fa-paper-plane"></i>
                            <span>Bulk Communication</span>
                        </a>
                        <% if (['SYSTEM_ADMIN', 'TRUST_ADMIN', 'SCHOOL_ADMIN'].includes(userRole)) { %>
                            <a href="/communication/templates" class="nav-link <%= currentPath === '/communication/templates' ? 'active' : '' %>">
                                <i class="fas fa-file-alt"></i>
                                <span>Templates</span>
                            </a>
                            <a href="/communication/analytics" class="nav-link <%= currentPath === '/communication/analytics' ? 'active' : '' %>">
                                <i class="fas fa-chart-line"></i>
                                <span>Analytics</span>
                            </a>
                            <a href="/communication/setup" class="nav-link <%= currentPath === '/communication/setup' ? 'active' : '' %>">
                                <i class="fas fa-cogs"></i>
                                <span>Setup</span>
                            </a>
                        <% } %>
                    </div>
                </div>
            <% } %>>
                            <i class="fas fa-paper-plane"></i>
                            <span>Compose</span>
                        </a>
                        <a href="/communication/templates" class="nav-link <%= currentPath === '/communication/templates' ? 'active' : '' %>">
                            <i class="fas fa-file-alt"></i>
                            <span>Templates</span>
                        </a>
                    <% } %>
                </div>
            </div>
            
            <!-- Parent Portal -->
            <% if (userRole === 'PARENT') { %>
                <div>
                    <div class="text-xs font-semibold text-muted uppercase tracking-wider mb-3">
                        My Children
                    </div>
                    <div class="space-y-1">
                        <a href="/parent/children" class="nav-link <%= currentPath === '/parent/children' ? 'active' : '' %>">
                            <i class="fas fa-child"></i>
                            <span>Children</span>
                        </a>
                        <a href="/parent/attendance" class="nav-link <%= currentPath === '/parent/attendance' ? 'active' : '' %>">
                            <i class="fas fa-calendar-alt"></i>
                            <span>Attendance</span>
                        </a>
                    </div>
                </div>
            <% } %>
        </nav>
        
        <!-- Footer -->
        <div class="mt-8 pt-6 border-t border-border">
            <div class="text-xs text-muted text-center">
                <div class="mb-2">
                    <i class="fas fa-graduation-cap"></i>
                    School ERP v1.0
                </div>
                <div>
                    © <%= year %> All rights reserved
                </div>
            </div>
        </div>
    </div>
</aside>

<style>
    .nav-link {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        padding: 0.75rem 1rem;
        border-radius: 0.5rem;
        color: var(--text-secondary);
        text-decoration: none;
        transition: all 0.2s ease;
        font-size: 0.875rem;
    }
    
    .nav-link:hover {
        background-color: var(--surface-alt);
        color: var(--text-primary);
    }
    
    .nav-link.active {
        background-color: var(--primary-50);
        color: var(--primary-600);
        border: 1px solid var(--primary-200);
        font-weight: 500;
    }
    
    .nav-link i {
        width: 1.25rem;
        text-align: center;
        font-size: 0.875rem;
    }
    
    /* Mobile responsive */
    @media (max-width: 768px) {
        .sidebar {
            transform: translateX(-100%);
            transition: transform 0.3s ease;
        }
        
        .sidebar.open {
            transform: translateX(0);
        }
        
        #mobileMenuToggle {
            display: inline-flex !important;
        }
    }
</style>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Mobile menu functionality
        const mobileMenuToggle = document.getElementById('mobileMenuToggle');
        const sidebar = document.querySelector('.sidebar');
        
        if (mobileMenuToggle && sidebar) {
            mobileMenuToggle.addEventListener('click', function() {
                sidebar.classList.toggle('open');
            });
            
            // Close sidebar when clicking outside on mobile
            document.addEventListener('click', function(e) {
                if (window.innerWidth <= 768) {
                    if (!sidebar.contains(e.target) && !mobileMenuToggle.contains(e.target)) {
                        sidebar.classList.remove('open');
                    }
                }
            });
        }
        
        // Add active state to current page
        const currentPath = '<%= currentPath %>';
        const navLinks = document.querySelectorAll('.nav-link');
        
        navLinks.forEach(link => {
            if (link.getAttribute('href') === currentPath) {
                link.classList.add('active');
            }
        });
    });
</script>