<!-- Default Navigation (for other roles) -->
<div class="bg-white border-bottom">
   <div class="container-fluid">
      <nav class="navbar navbar-expand-lg navbar-light p-0">
         <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse" data-bs-target="#defaultNav">
            <span class="navbar-toggler-icon"></span>
         </button>

         <div class="collapse navbar-collapse" id="defaultNav">
            <ul class="navbar-nav me-auto">
               <li class="nav-item">
                  <a class="nav-link <%= currentPath === '/dashboard' ? 'active' : '' %>" href="/dashboard">
                     <i class="fas fa-home me-1"></i>Dashboard
                  </a>
               </li>
               <li class="nav-item">
                  <a
                     class="nav-link <%= currentPath && currentPath.includes('/profile') ? 'active' : '' %>"
                     href="/profile">
                     <i class="fas fa-user me-1"></i>Profile
                  </a>
               </li>
               <li class="nav-item">
                  <a
                     class="nav-link <%= currentPath && currentPath.includes('/settings') ? 'active' : '' %>"
                     href="/settings">
                     <i class="fas fa-cog me-1"></i>Settings
                  </a>
               </li>
            </ul>

            <!-- Right side nav items -->
            <ul class="navbar-nav">
               <li class="nav-item">
                  <a class="nav-link" href="/help"> <i class="fas fa-question-circle me-1"></i>Help </a>
               </li>
            </ul>
         </div>
      </nav>
   </div>
</div>
